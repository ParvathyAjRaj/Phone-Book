<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Summary</title>
    <link rel="stylesheet" href="/styles/new.css">
</head>
<body>
    <hr>
    <h1>Contact List</h1>
    <hr>
    <br>
    <div class="summary">
        <ul class="all_contacts"> 
            <% if (locals.details) {%>
                <% for( var i = 0;i<details.length;i++){ %>
                       <% let id = details[i].id;%>
                       <% let name = details[i].name; %>
                       <% let phone_num = details[i].contact %>
                        <div class="box">
                            <div>
                                <dl>
                                    <dd  class="each_student"><h4><img class="image" src="/images/person.png" alt="person">  <%= name %></h4></dd>
                                    <dd  class="each_student"><h4><img class="image" src="/images/phone.png" alt="phone"><%= phone_num %></h4></dd>
                                </dl>
                            </div>
                        </div>
                        <br>
                   <% } %>
           <% } %>
        </ul>
    </div>
    <div class="button">
        <a class="btn" id="home" href="/home">Home Page</a>
    </div>
</body>
</html>